# =============================================================================
# PROMOTED — this workflow is now ACTIVE (manual / workflow_dispatch) at the canonical location:
#
#     .github/workflows/yearline_nightly.yml     (V13.8.4)
#
# This path is retained only so older doc links still resolve. Do NOT edit this copy — edit the
# canonical one under .github/workflows/. (GitHub only runs workflows in .github/workflows/, so this
# file is inert.)
#
# Status: workflow_dispatch-first (manual). The `schedule:` trigger stays commented until a data-API
# secret (DATA_API_KEY) is configured AND a scripts/run_nightly.py entrypoint exists.
# Enablement checklist + rationale: ../option_mgmt_handoff.md §10.
# =============================================================================
promoted_to: .github/workflows/yearline_nightly.yml
status: active_manual_workflow_dispatch_first
schedule_enabled: false
